# Experimental — tier 1

**Voice.** Choose the register first. A spatial, generative or physics world carries its bespoke engine across element classes; a research-publication register carries bespoke type, diagrams and reading navigation through a native document, with no engine required. Each reads as one authored world. For an engine register, cohesion comes from one active-state signal and one easing family; positional audio belongs only to a declared spatial sound direction. Variety lives in the per-class mechanic. For a reading register, composition, annotation and typographic relationships carry that variety without requiring motion.

**Register licence.** Legibility and usable mobile navigation bind every register. Engine examples include Lusion v3 (Developer Site of the Year 2023, Animations 10.00), Shader Development Studio (SOTD 2026-04-20, 7.73; Developer Award 8.12, Responsive 8.40) and Bruno's Portfolio (8.11 overall, Creativity 8.62). They demonstrate ambitious scenes that remain operable. A live engine stays responsive between inputs; physics may settle and rendering pauses when hidden or required by reduced-motion policy. Research-publication may stay static. Preserve a discoverable orientation anchor, and cap declared amplitude peaks at one or two replayable ones without adding peaks to a static reading direction.

**Anti-signals — these disqualify.**
Apply engine and kinetic anti-signals only to their declared registers. In research-publication, static diagrams, conventional reading links and a document index may be the committed medium; judge their craft and purpose rather than importing engine requirements.

- A pale washed-out tint fill on button hover — a 10–20% brand-alpha sweep is the single clearest tell.
- A scrolled solid nav bar with a contrasting `border-bottom`: a corporate pattern that never ports here.
- One identical hover cloned onto every element class; a single CSS trick stamped on an otherwise normal DOM.
- A CSS underline-draw as the link move. That belongs to editorial; this line decodes, scrambles or warps.
- Generic `fade-up 20px` as the only reveal; a `background-clip:text` gradient sold as kinetic type.
- A spinner or a bare percent counter with no tie to the concept, then a hard cut into the page.
- A `mix-blend-mode` lag-dot cursor that adds no mechanic, on a page where the pointer already has a job.
- A card-grid fold, stock photography, a footer sitemap.
- The frozen canvas: a hero shader that stops after boot, a WebGL field that renders on scroll-in then parks. If a still image could replace the canvas without loss, the substrate was never built.

**Macrostructures it runs.**

- Research-publication — a native reading document with custom type, diagrams, citations and linked essays; an index leads to articles. Route here for research labs and essayists. Thesephist and Ink & Switch are documented references, not verified award winners.
- `engine-world`, free-roam — no scroll; the funnel jobs collapse into an operable 3D space and landmarks are the nav. Route here when the brief names a verb the visitor performs: drive, walk, pilot. (Bruno's Portfolio, winner-verified)
- `engine-world`, journey — one continuous in-engine scene whose section breaks are shader seams; the wheel drives a camera, not a document. Route here when the brief supplies discrete projects that can each own a zone. (Igloo Inc SOTY 2024; Lusion v3 Developer SOTY 2023)
- `type-index` — a corner counter boots straight into a giant-type index over a live WebGL field; no marketing hero, no prose, and the climax is on-click. Route here when a body of named work is itself the argument. (Aristide Benoist, winner-verified)
- Physics field — elements are rigid bodies with weight and restitution that you grab, throw and tumble; the simulation is the medium and the reward. Route here when the brief's objects have tactile identity. It runs the `engine-world` shape on a 2D engine. (MoMoney, winner-verified)

**Exemplar.**

| site | award | signature |
|---|---|---|
| Bruno's Portfolio · `bruno-simon.com` | Awwwards SOTD 2026-01-21 — 8.11 overall, Creativity 8.62; Developer Award 7.65 with Animations 8.60 and Responsive 8.20; Site of the Month January 2026 | You drive a physics vehicle through a hand-built town to find the work — no scroll, no menu, no grid, the landmarks are the navigation. Standard DOM events are recreated as 3D raycasts so the world stays touch-, keyboard- and gamepad-navigable; live weather, day/night and seasons run for every visitor at once, up to 30 other visitors' Whispers drift through the shared space, and spatialized audio pans with the camera. |

**Reflexes — enumerate, then reject by name before committing.**
1. A dark page with a full-screen shader-noise background, conventional DOM sections scrolling over it untouched.
2. Three.js boilerplate: a rotating distorted icosahedron or torus knot parked behind the hero headline.
3. A `mix-blend-mode: difference` lag-dot cursor offered as the entire evidence that the build is custom.
4. A particle field that answers the mouse and nothing else, inert for the rest of the page.
5. "Experimental" delivered as an oversized-type marquee plus a glitch or RGB-split filter on hover.
6. A percentage preloader over the canvas, then a hard cut into a conventional landing page underneath.
7. Scroll-jacking presented as a bespoke navigation metaphor, with no anchor telling the visitor what to do.
8. The whole budget spent on the hero canvas; the mid-page reverting to cards, icons and fade-ups.
