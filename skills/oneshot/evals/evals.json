{
  "skill_name": "oneshot",
  "description": "Focused edits, verification, scope preservation and replanning when needed.",
  "evals": [
    {
      "id": 1,
      "name": "single-file-trivial-edit",
      "prompt": "Add a console.log of the user id at the top of the login function in src/auth/login.ts.",
      "expected_output": "Single-file edit. Workflow: brief Glob/Grep to locate the function, Edit to insert the log, run lint + typecheck. Output report follows the documented format: ## Done block with Task, Files changed, Validation lines.",
      "files": [
        "src/auth/login.ts"
      ]
    },
    {
      "id": 2,
      "name": "resolves-github-issue-reference",
      "prompt": "/oneshot #42",
      "expected_output": "Resolves the issue via `gh issue view 42 --json title,body,labels`. Uses the issue title + body as task description. If the body has acceptance criteria, treats them as the implementation checklist.",
      "files": []
    },
    {
      "id": 3,
      "name": "circuit-breaker-fires-on-multi-file",
      "prompt": "Rewrite the authentication system to support OAuth, magic links, and SAML SSO across the API, the web app, and the mobile app.",
      "expected_output": "Explain why broader planning is needed, then continue authorized work using /apex when available. Pause only for missing authorization or a user-owned decision; file count alone is not a blocker.",
      "files": []
    },
    {
      "id": 4,
      "name": "circuit-breaker-fires-on-cross-cutting",
      "prompt": "Add a new column to the users table — store last_active timestamp and update the auth flow + admin dashboard to display it.",
      "expected_output": "Inspect migration conventions, dependent callers, verification and rollback, then continue authorized implementation using the appropriate workflow.",
      "files": []
    },
    {
      "id": 5,
      "name": "stays-in-scope",
      "prompt": "Fix the typo in the welcome email template (welcome.txt).",
      "expected_output": "Single-file Edit, no tangential improvements. Does NOT refactor the surrounding template, does NOT add comments, does NOT improve unrelated style. Output report shows exactly one file changed.",
      "files": [
        "welcome.txt"
      ]
    },
    {
      "id": 6,
      "name": "recovery-follows-progress",
      "prompt": "Update the deployment script to use the new Cloudflare Workers API. (The script has unfamiliar legacy patterns.)",
      "expected_output": "Use current documentation and specific diagnostics to repair the failure. Continue while a new concrete experiment can advance the task; report exact evidence only when a real blocker remains.",
      "files": []
    },
    {
      "id": 7,
      "name": "assessment-only-stays-read-only",
      "prompt": "Use /oneshot only to assess whether correcting the password-reset redirect fits a small change. Do not edit files yet.",
      "expected_output": "Inspects the redirect handler and affected callers, explains the smallest complete change and applicable checks, then stops and reports the assessment. Preserves the explicit read-only boundary; the workflow's implementation default does not authorize edits.",
      "files": []
    },
    {
      "id": 8,
      "name": "reuse-existing-helper",
      "prompt": "/oneshot add email validation to the signup handler: a malformed email raises a ValidationError on the email field. The repository already has an unused isValidEmail helper and a ValidationError class.",
      "expected_output": "Before writing code, walks the build ladder in references/quality-lens.md, finds the repository's existing isValidEmail helper and ValidationError class and reuses them. The diff adds no email regex, helper or file, adds one focused test, and no comments. The Done report carries a Refine line.",
      "files": []
    },
    {
      "id": 9,
      "name": "no-speculative-options",
      "prompt": "/oneshot add a toCsv(columns, rows) helper for the admin report export, with a test. Values may contain commas, quotes or newlines.",
      "expected_output": "Writes a plain toCsv function that escapes commas, quotes and newlines in values. Adds no delimiter or format option, class, factory or restating comments. One focused test covers the header and escaping. Refine re-reads the diff against references/quality-lens.md, and the Done report carries a Refine line.",
      "files": []
    },
    {
      "id": 10,
      "name": "non-code-calculation-check",
      "prompt": "/oneshot refresh report.md from the confirmed counts: earlier 28 completed out of 40 received; later 40 out of 50. Keep the table, a short summary and the caveat that intake criteria changed. The current report calls this a 10% improvement. Edit only the report.",
      "expected_output": "Calculates 70% and 80%, states the change as 10 percentage points and retains the comparability caveat without claiming a proven process improvement. Checks the calculations, edits only the report and does not add a software test suite or claim nonexistent checks passed.",
      "files": []
    }
  ]
}
