# Spec — scope-add fixture

A minimal spec with one AC. The diff (out of band) implements that AC
plus an additional feature the spec does not mention. The subagent
classifies the extra feature as SCOPE-ADD.

## Acceptance criteria

- [ ] AC1: the parser handles UTF-8 input
