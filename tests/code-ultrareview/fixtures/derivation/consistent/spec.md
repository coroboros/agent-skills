# Spec — consistent fixture

Every claim has a matching reference in the (out-of-band) diff. The
subagent classifies all as CONSISTENT.

## Acceptance criteria

- [ ] AC1: the CLI accepts `--input <path>`
- [ ] AC2: a missing file returns exit code 2
