# Spec — allowlisted fixture

The fixture's `.derivation-ignore` allowlists this `spec.md` by path. The
lens emits no finding rows for it.

## Acceptance criteria

- [ ] AC1: a claim that would otherwise be UNCLASSIFIED
- [ ] AC2: another suppressed claim
