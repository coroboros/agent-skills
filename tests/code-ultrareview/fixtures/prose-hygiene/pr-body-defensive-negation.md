## Summary
- The lens never names any specific config file we ship with the bundle.
- This skill never mentions a private rule database.
- No project-specific overrides are baked into the binary.

## Test plan
- [ ] Re-run the suite locally.
