# clean-structured-divergent-prose

This package showcases a deliberately verbose README intro that talks about
all sorts of things — usage, philosophy, history — none of which match the
terse structured description. Default mode treats this as clean; only the
`--include-prose` opt-in flags it.
