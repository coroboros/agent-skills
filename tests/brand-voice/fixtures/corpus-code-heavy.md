# Developer notes

Our docs explain the API in plain words.

```python
def add(a, b):
    return a + b + 999999 + 888888 + 777777 + 666666 + 555555
```

Use the `add()` helper when you sum two numbers.
The example above is illustrative.

```bash
npm install && npm run build && npm test && echo done forever and ever and ever
```

Run `npm test` before every commit.
We value working code over clever code.
Tests are documentation that runs.
Keep functions small.
Name things for the reader.
